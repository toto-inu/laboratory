import React, { FunctionComponent, ReactElement } from 'react';

const Chat: FunctionComponent = (): ReactElement => {
  return (
    <div className="Chat">
      Chat画面
    </div>
  );
}

export default Chat;
